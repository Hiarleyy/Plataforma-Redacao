const { v4: uuid } = require("uuid");

class Modulos {
  constructor(modulos) {
    this.id = uuid();
    this.nome = modulos.nome;
    this.descricao = modulos.descricao;
    this.playlist_url = modulos.playlist_url;
    this.data_criacao = modulos.data_criacao;
  }
}

module.exports = Modulos;
